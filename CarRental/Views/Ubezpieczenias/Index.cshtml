@model IEnumerable<CarRental.Models.Ubezpieczenia>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.IDUbezpieczenia)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.NazwaUbezpieczenia)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.WartoscUbezpieczenia)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.IDUbezpieczenia)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.NazwaUbezpieczenia)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.WartoscUbezpieczenia)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id = item.IDUbezpieczenia }) |
            @Html.ActionLink("Details", "Details", new { id = item.IDUbezpieczenia }) |
            @Html.ActionLink("Delete", "Delete", new { id = item.IDUbezpieczenia })
        </td>
    </tr>
}

</table>
